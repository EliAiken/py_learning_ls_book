list1 = [2, 6, 4]
list2 = [2, 6, 4]

print(list1 == list2)

# True because the values of each list are the same, however they are
# not the same object, so the `is` function would return False

# == in python measures "value equality"
# `is` in python measures "object equality"